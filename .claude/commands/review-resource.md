# Review a governed resource

Validate the resource manifest identified by the user, then summarize its purpose, lifecycle,
dependencies, permissions, context requirements, success criteria, provenance, and evaluation
coverage. Report digest or version mismatches without editing the resource.

For a frozen resource, recommend a successor version rather than an in-place change. Never promote,
approve, retire, or execute the resource without a separate explicit user request.
